= form_tag admins_stores_path, method: :get do
  = select_tag :progress_id,\
    options_from_collection_for_select(Progress.all, :id, :name, params[:progress_id]),\
    { prompt: '< 選択 >', class: 'form-control', onchange: 'submit(this.form);' }
